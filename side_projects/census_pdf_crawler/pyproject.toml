[project]
name = "census-pdf-crawler"
version = "0.1.0"
description = "Crawl Census publication child pages and download PDFs"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "beautifulsoup4>=4.13.4,<5",
  "httpx>=0.28.1,<0.29",
]

[tool.uv]
package = false
